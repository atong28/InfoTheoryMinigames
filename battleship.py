import argparse
import time
import os

class Battleship():

    def __init__(self):
        pass


if __name__ == '__main__':
    parser = argparse.ArgumentParser(description='Battleship Tester')
    # args = parser.parse_args()

    '''arguments = [args.username, args.password, args.ip]
    if (args.debug):
        try:
            arguments.append(bool(args.debug))
        except Exception:
            print("Potentially invalid debug argument. Please enter True or False")
    '''

    run = Battleship()